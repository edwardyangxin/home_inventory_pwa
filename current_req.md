我的新需求如下：
1. 可以支持超市采购小票的图片的识别》自动录入的inventory
  1. 英文的小票为主，翻译为中文条目，为了好检索
  2. 每个条目识别出来，尤其是quantity or 重量要识别出来
  

api repo要做的是：
1. 创建必要的api
2. 根据需求点设计api
3. 在本地做好测试
4. 如果遇到循环卡点，那么及时停下来，总结当前做了什么、和情况，让我来做下一步的决策

补充与实现总结：
1. 已新增 `/ingestReceipt` 支持 `multipart/form-data` 上传小票图片，调用 Gemini OCR 将英文条目翻译为中文，并识别数量/重量后自动入库。
2. OCR 支持将类似 `2 x 500g` 原始信息写入 `comment` 字段，优先识别数量，其次重量/体积；库存表新增 `comment` 列用于保留原始信息。
3. 本地测试已完成：`sample_receipt.jpg` 入库成功；`searchInventory` 支持模糊搜索（含 comment），查询“华夫饼”可命中“Eggo 华夫饼”。

作为前端PWA工程repo，你要参考api的实现，实现UI部分：
1. 在录入的框区域增加一个上传购物小票的功能，然后去识别，显示识别的效果，倒计时5s，自动更新库存。
2. 做好uer_manual.md的更新
3. 在本地做好测试
4. 如果遇到循环卡点，那么及时停下来，总结当前做了什么、和情况，让我来做下一步的决策